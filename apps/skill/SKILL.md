---
name: clawd-match
description: Send your Claude Code history to clawd.date to find devs you'd vibe with. Invoke when the user asks to "find dev matches", "see who I'd pair well with on clawd.date", or to update their clawd.date profile.
---

This skill bundles a snapshot of the user's Claude Code activity (languages, tools, project summaries) and POSTs it to the clawd.date Convex backend, then surfaces mutual matches.

<!-- TODO: implement skill body — collection logic, prompts, and follow-up actions. -->
