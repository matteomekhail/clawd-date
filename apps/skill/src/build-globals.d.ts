// Substituted at build time by esbuild `define` (see build.mjs). The values
// are read from CLAWD_{PROD,DEV}_API_URL in the environment / .env.local.
declare const __PROD_API_URL__: string;
declare const __DEV_API_URL__: string;
