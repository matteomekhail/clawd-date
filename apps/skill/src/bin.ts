import { sendIngest } from "./index.js";

async function main(): Promise<void> {
  // TODO: collect Claude Code history (profile + sessions) from local files
  // and pass it to sendIngest. For now this is a stub.
  void sendIngest;
}

void main();
